import { styled } from "styled-components";

const ChannelBody = () => {
    return (
        <ChannelBodyStyle>
            <h1>ChannelBody</h1>
        </ChannelBodyStyle>
    );
};

const ChannelBodyStyle = styled.div``;

export default ChannelBody;
