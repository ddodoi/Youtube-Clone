import { styled } from "styled-components";

const ChannelVideos = () => {
    return (
        <ChannelVideosStyle>
            <h1>ChannelVideos</h1>
        </ChannelVideosStyle>
    );
};

const ChannelVideosStyle = styled.div``;

export default ChannelVideos;
